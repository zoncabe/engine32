ENGINE_DIR   = ../..
PROJECT_NAME = 01_scene_3d

# The frame's cost out the BIOS teletype, one line a second, which ares
# prints on its own terminal. Off, nothing is built and nothing is called.
cflags_extra = -DE32_TRACE

include $(ENGINE_DIR)/engine32.mk
